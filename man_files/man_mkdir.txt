MKDIR(1)                                    SEA SHELL COMMAND MANUAL                                      MKDIR(1)

NAME
	makedir -- make a directory

SYNOPSIS
	makedir PARENT_DIRECTORY DIRECTORY_NAME
	makedir [--help]

DESCRIPTION
	Create a directory in the PARENT_DIRECTORY with the name DIRECTORY_NAME, if they donot already exist

	optional parameters
		perm -- permissions for the created directory(default = 0777)

AUTHOR
	Jayanth Shanmugam	
